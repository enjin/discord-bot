{
  "name": "discord-bot",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && bun build --target=bun --root . backend/index.ts --outdir=dist",
    "dev": "run run backend/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/qrcode": "^1.5.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.44",
    "@walletconnect/sign-client": "^2.11.0",
    "discord.js": "^14.14.1",
    "qrcode": "^1.5.3",
    "zod": "^3.22.4"
  }
}
